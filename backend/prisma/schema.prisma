// =========================================
// Prisma Schema cho hệ thống đặt vé xe khách
// Database: PostgreSQL
// =========================================

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//
// MODELS
//

model User {
  id        Int      @id @default(autoincrement())
  uid       String   @unique                  // UID từ Firebase
  name      String
  email     String   @unique
  phone     String?
  isActive  Boolean  @default(true)
  role      Role     @relation(fields: [roleId], references: [id])
  roleId    Int
  tickets   Ticket[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([roleId])
}

model Role {
  id    Int      @id @default(autoincrement())
  name  RoleName @unique
  users User[]
}

model Brand {
  id          Int       @id @default(autoincrement())
  name        String    @unique                 // Tên nhà xe
  phoneNumber String?                           // SĐT liên hệ
  image       String?                           // Ảnh logo hoặc banner (có thể null)
  address     String?                           // Địa chỉ nhà xe
  buses       Bus[]                             // Danh sách xe thuộc nhà xe
  routes      Route[]                           // 🔹 Danh sách tuyến nhà xe phục vụ
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([name])
}

model Bus {
  id           Int        @id @default(autoincrement())
  name         String                              // Tên xe
  licensePlate String     @unique                   // Biển số
  seatCount    Int                                  // Số ghế
  type         BusType                              // Loại xe
  brand        Brand      @relation(fields: [brandId], references: [id])
  brandId      Int
  schedules    Schedule[]                           // Các chuyến chạy của xe
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt

  @@index([type])
  @@index([brandId])
}

model Route {
  id                 Int        @id @default(autoincrement())
  startPoint         String                              // Nơi khởi hành
  endPoint           String                              // Nơi đến
  averageDurationMin Int                                 // Thời lượng trung bình (phút)
  lowestPrice        Float                               // Giá vé thấp nhất
  distanceKm         Float?                              // Quãng đường (km)
  image              String?                             // Hình minh họa tuyến đường hoặc bến xe
  brand              Brand?      @relation(fields: [brandId], references: [id])
  brandId            Int?                                // Nhà xe chính phục vụ tuyến
  schedules          Schedule[]                          // Các chuyến chạy cụ thể
  createdAt          DateTime   @default(now())
  updatedAt          DateTime   @updatedAt

  @@index([startPoint])
  @@index([endPoint])
}

model Schedule {
  id          Int            @id @default(autoincrement())
  bus         Bus            @relation(fields: [busId], references: [id])
  busId       Int
  route       Route          @relation(fields: [routeId], references: [id])
  routeId     Int
  departureAt DateTime                                // Thời gian khởi hành
  arrivalAt   DateTime                                // Thời gian đến
  status      ScheduleStatus @default(UPCOMING)        // Trạng thái chuyến
  tickets     Ticket[]
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt

  @@index([busId])
  @@index([routeId])
  @@index([status])
}

model Ticket {
  id            Int            @id @default(autoincrement())
  user          User           @relation(fields: [userId], references: [id])
  userId        Int
  schedule      Schedule       @relation(fields: [scheduleId], references: [id])
  scheduleId    Int
  seatNumber    Int                                     // Số ghế đã đặt
  price         Float                                   // Giá vé
  status        TicketStatus   @default(BOOKED)         // Trạng thái vé
  paymentMethod PaymentMethod?                          // Phương thức thanh toán
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt

  @@index([userId])
  @@index([scheduleId])
  @@index([status])
}

//
// ENUMS
//

enum RoleName {
  ADMIN
  PASSENGER
}

enum BusType {
  MINIVAN_16
  COACH_30
  COACH_45
  LIMOUSINE
}

enum ScheduleStatus {
  UPCOMING
  ONGOING
  COMPLETED
  CANCELLED
}

enum TicketStatus {
  BOOKED
  PAID
  CANCELLED
}

enum PaymentMethod {
  CASH
  CREDIT_CARD
  MOMO
  ZALOPAY
}
